export const unstable_instant = { prefetch: 'static' };

import { NetworkingEngine } from "@/components/NetworkingEngine";

export default function NetworkingPage() {
  return <NetworkingEngine />;
}
